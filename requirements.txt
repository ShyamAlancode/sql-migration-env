# SQL Migration Safety Gym — Python Dependencies
# OpenEnv Hackathon 2026 Submission
#
# Install: pip install -r requirements.txt

# Core OpenEnv framework (MANDATORY — matches official course requirements)
openenv-core>=0.2.2

# Server-side dependencies
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0
python-multipart>=0.0.6

# HTTP client for inference script
requests>=2.31.0
httpx>=0.26.0

# LLM inference (OpenAI-compatible client — MANDATORY per spec)
openai>=1.10.0

# Environment utilities
python-dotenv>=1.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
